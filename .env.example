# Copy to .env.local and edit as needed
# Websocket server URL (server-only):
WEBSOCKET_SERVER_URL=''

# Frontend base URL (exposed to the browser) - must use NEXT_PUBLIC_ prefix
NEXT_PUBLIC_FRONTEND_URL=''